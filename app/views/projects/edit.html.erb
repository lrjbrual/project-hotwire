<%= turbo_frame_tag dom_id(@project) do %>
  <div class="card">
    <div class="card-body">
      <%= render 'form', project: @project %>
      <%= link_to "cancel", projects_path %>    
    </div>
  </div>
<% end %>